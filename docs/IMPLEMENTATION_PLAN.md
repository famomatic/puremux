# Puremux Implementation Ledger

This document tracks the live progress of the `puremux` project.
**Rule:** AI agents MUST update the Status, Date, and Notes immediately after completing a task.

## Design Decisions Locked (2026-06-29)

These decisions are binding on all subsequent implementation work. They are
recorded here so the Progress Ledger and the architecture doc stay in sync.

1. **Packet opacity is scoped, not absolute.** Codec *packet header* parsing
   (VP9 superframe index, AV1 OBU header, Opus frame count) is permitted and
   lives behind the `CodecKeyframeDetector` interface in `internal/core`.
   Media *decoding* (pixels / PCM) is permanently forbidden. See
   ARCHITECTURE.md §4.
2. **Jitter buffer is bounded.** `MaxBufferSize` and `MaxBufferDuration` are
   configurable. Overflow drops the oldest out-of-order packet and records a
   metric. No unbounded absorption. See ARCHITECTURE.md §5.B.
3. **Audio realignment is packet-granular only.** The Keyframe Aligner may
   drop or duplicate whole Opus packets but MUST NOT trim within a packet.
4. **SeekHead + Cues + Duration patch** for seekable writers; streaming
   sentinel + omitted Cues for non-seekable writers. See ARCHITECTURE.md §5.C.
5. **Single-writer concurrency model.** Per-track channels merge into one
   writer goroutine. Preprocessors are per-track and stateful.
6. **WebM first, MKV as superset.** No MKV-specific work until a VP9+Opus
   WebM round-trips against a reference player.

## Media Expansion Decisions Locked (2026-09-03)

1. The historical `pkg/puremux` mux facade remains source-compatible while a
   new `pkg/media` API is built. The module path is not renamed mid-cycle
   because ytv1 and lavalink currently depend on it.
2. Public demux timestamps use signed integer stream ticks plus an exact
   rational time base. `time.Duration` is convenience-only.
3. Demuxers preserve container timestamps and never invoke timestamp repair.
4. Packet ownership, cancellation, seek invalidation, and source capabilities
   are public API contracts, not implementation details.
5. HTTP is a source transport. HLS and DASH are manifest layers that delegate
   segment bytes to format demuxers.
6. Codec configuration and bitstream framing may be parsed; decoding,
   encoding, PCM/pixel inspection, resampling, and DSP remain forbidden.

## Progress Ledger

| Phase / Task                                          | Status  | Updated Date | Assignee   | Notes                                              |
| :---------------------------------------------------- | :------ | :----------- | :--------- | :------------------------------------------------- |
| **Phase 0: Design & Docs**                            |         |              |            |                                                    |
| Architecture + Implementation Plan scaffold          | Done    | 2026-06-29   | glm-5.2    | Initial docs from project kickoff                  |
| Architecture reinforcement (opacity, jitter, concur) | Done    | 2026-06-29   | glm-5.2    | Added §4-§7: opacity table, CodecKeyframeDetector, bounded jitter, streaming mode, concurrency model, WebM-first gate |
| **Phase 1: Setup & Core**                             |         |              |            |                                                    |
| Go Module Init & Directory Scaffold                  | Done    | 2026-06-30   | glm-5.2    | module famomatic/puremux (renamed from github.com/mjmst/puremux), Go 1.23.5; scaffolded cmd/pkg/internal tree |
| Define internal/core (Packet, Codecs)                | Done    | 2026-06-30   | glm-5.2    | Packet/CodecType/Track/sync.Pool; VP8/VP9/AV1 detectors behind CodecKeyframeDetector. Verification: spec-derived bytes, MSB-first confirmed for AV1, boundary cases (nil/empty/truncated/forbidden-bit) covered, no panics. CGO_ENABLED=0 build+vet+tests green |
| Define base Interfaces (Muxer, Preprocessor)         | Done    | 2026-06-30   | glm-5.2    | muxer.Muxer/Writer/Factory; preprocessor.Preprocessor/Config/Metrics; CGO_ENABLED=0 build + vet + tests green |
| **Phase 2: EBML Engine**                             |         |              |            |                                                    |
| Implement EBML Element Writer (VINT, etc.)            | Done    | 2026-06-30   | glm-5.2    | ebml: VINT encode/decode, ElementID (id includes marker bit, classes 1-4 verified vs WebM IDs), EncodeElement/UnknownSize, writer helpers. Verification: RFC 8794 spec-derived bytes, width 1-8 roundtrips, unknown-size sentinels, boundary cases, no panics |
| Implement EBML Parser (for seeking/patching)          | Done    | 2026-06-30   | glm-5.2    | ebml: ReadHeader (ID+size decode), PatchSize (reserved width overwrite for graceful closer), Header bookkeeping. Verification: spec-derived Cluster/Segment headers, patch roundtrip, overflow + bounds cases |
| **Phase 3: Preprocessor**                            |         |              |            |                                                    |
| Monotonic Timestamp Enforcer                          | Done    | 2026-06-30   | glm-5.2    | preprocessor.Enforcer: bounded jitter buffer (MaxBufferSize/MaxBufferDuration), time-based hold + Flush commit, overflow drops oldest + DroppedOverflow metric, late-packet drop, strict monotonicity nudge. Verification: out-of-order reorder, late drop, overflow drop (no Flush, large window), reset, monotonic order confirmed |
| Keyframe Aligner                                      | Done    | 2026-06-30   | glm-5.2    | preprocessor.Aligner: video drops until first keyframe (via CodecKeyframeDetector, never inline byte probe), packet-granular audio drop (whole-packet only, no trim per §5.B), SetVideoSyncStart propagation. Verification: drop-until-keyframe, pass-after-keyframe, audio packet-granular drop with data-intact check (trim forbidden), audio-only passthrough, reset |
| **Phase 4: WebM Muxer**                              |         |              |            |                                                    |
| WebM Header Initialization                            | Done    | 2026-06-30   | glm-5.2    | webm: EBML header (doctype webm v4), BeginSegment (seekable reserved 8-byte size / streaming unknown sentinel), WriteInfo (TimecodeScale + reserved Duration float slot). Verification: spec-derived Segment ID 0x18538067, reserved size width-8 = 0x01+7zeros, streaming sentinel = 0x01+7*0xFF, Duration payload offset confirmed via mock seeker |
| Cluster & Block Serialization                         | Done    | 2026-06-30   | glm-5.2    | webm: SimpleBlock (TrackNumber VINT + int16 BE timecode + flags + opaque payload), ClusterWriter (BeginCluster/WriteSimpleBlock/Close, reserved 4-byte size patched on close, streaming unknown sentinel). Verification: spec-derived bytes (track1=0x81, tc100=0x0064, keyframe=0x80, neg tc -100=0xFF9C, wide track 200=0x40 0xC8, int16 bounds), size patch roundtrip, double-close safe |
| Graceful Closer (Seek & Patch Duration/Cues)          | Done    | 2026-06-30   | glm-5.2    | webm: PatchDuration (IEEE-754 double BE overwrite), WriteCues (CuePoint/CueTrackPositions), WriteSeekHead (Info/Tracks/Cues pointers, streaming omits Cues), WriteTracks (TrackEntry/Video/Audio). Verification: end-to-end test generates complete WebM (EBML header+Segment+Info+Tracks+Cluster+SimpleBlock+Cues), Segment size patched, Duration=1.0s patched to 0x3FF0000000000000, SeekHead pointers, spec-derived values confirmed |
| **Phase 5: CLI & Facade**                            |         |              |            |                                                    |
| Build `pkg/puremux` (Public API)                     | Done    | 2026-06-30   | glm-5.2    | pkg/puremux.Session: NewSession (auto seekable/non-seekable), AddTrack, WritePacket (Enforcer->Aligner->Cluster pipeline, per-track preprocessors, video sync propagation), Close (PatchDuration/Cues/PatchSegmentSize seekable; streaming sentinel otherwise). Verification: seekable VP9+Opus end-to-end (webm/V_VP9/A_OPUS/Cluster/SimpleBlock present, 240B output), streaming unknown-size sentinel, unknown-track error, idempotent close |
| Build `cmd/puremux` (CLI wrapper)                     | Done    | 2026-06-30   | glm-5.2    | cmd/puremux: minimal entrypoint with -h/--help usage; thin shell over the library API. CGO_ENABLED=0 build green |
| **Phase 9: Audit Fixes (Bug/Edge-case/Structural)** |         |              |            |                                                    |
| Session: segmentTc0 dead code + SeekHead + relTc overflow | Done    | 2026-07-12   | glm-5.2    | pkg/puremux/session.go: segmentTc0 was never set (writeBlock's !started branch was dead because WritePacket sets started=true first); added segTc0Set flag captured from first emitted packet so cue timecodes are now relative to the first packet DTS, not absolute. Close() now writes SeekHead (ARCHITECTURE.md §5.C required it but only Cues were emitted); added InfoStart/TracksStart to webm.Header. relTc computed via signed int64 (was uint64 subtraction relying on underflow+int16 truncation coincidence; added <-32768 guard for cluster rollover). Verification: SeekHead element (0x114D9B74) present in seekable output, segmentTc0 captured for non-zero first DTS, existing seekable/streaming tests green. CGO_ENABLED=0 build+vet+tests green |
| WebM reader: readChildHeader idWidth dead call         | Done    | 2026-07-12   | glm-5.2    | internal/format/webm/reader.go: readChildHeader called ebml.VINTWidth(0) (always 0) then overwrote via switch; replaced with ebml.ElementIDWidth(id) helper added to ebml package. Verification: pos-tracking test confirms correct ID width 2 + size width 1 = 3 bytes consumed. CGO_ENABLED=0 build+vet+tests green |
| Enforcer: slice compaction + metric rename             | Done    | 2026-07-12   | glm-5.2    | internal/preprocessor/enforcer.go: insertOrdered/flushReady used e.buf=e.buf[1:] / e.buf=e.buf[i:] which left dropped/emitted packet pointers in the backing array; replaced with copy + nil-clear + reslice. InterpolatedGaps renamed to DetectedGaps (no synthesis occurs; the old name implied interpolation). Verification: monotonic order, late drop, overflow drop, reset tests green. CGO_ENABLED=0 build+vet+tests green |
| Aligner: audio hold-until-video-sync                   | Done    | 2026-07-12   | glm-5.2    | internal/preprocessor/aligner.go: audio tracks in video-paired sessions previously emitted all packets before SetVideoSyncStart arrived (leaking audio before the video sync point); added bounded pending queue (maxPending=64, overflow drops oldest) + NewAlignerForSession + SetExpectsVideoSync so audio-only sessions flush. session.go writeHeader resolves hasVideo and clears the flag for audio-only. Verification: hold-until-sync (early packet dropped, on-time emitted), audio-only flush tests green. CGO_ENABLED=0 build+vet+tests green |
| Session: pool packet release                           | Done    | 2026-07-12   | glm-5.2    | pkg/puremux/session.go: emitted packets from the Enforcer->Aligner pipeline were never returned to the sync.Pool; added core.ReleasePacket(ap) after writeBlock (synchronous serialization means Data is no longer referenced). Verification: all session tests green, no double-release (Aligner releases dropped packets itself). CGO_ENABLED=0 build+vet+tests green |
| WebM header: dedup + encodeUnknownSize removal         | Done    | 2026-07-12   | glm-5.2    | internal/format/webm/header.go: WriteEBMLHeader is now a thin wrapper over WriteEBMLHeaderFor("webm"); removed duplicated body. encodeUnknownSize (local reimplementation of ebml.EncodeVINTUnknown) removed from header.go + cluster.go, replaced with ebml.EncodeVINTUnknown. Removed dead `off` variable in WriteInfo. Verification: e2e + header + cluster tests green. CGO_ENABLED=0 build+vet+tests green |
| NAL startCodeLenAt: dead branch removal                | Done    | 2026-07-12   | glm-5.2    | internal/core/codec_nal.go: startCodeLenAt had a dead inner if (data[i+3]==0 after data[i+2]==1 already matched) with misleading comment; reordered to check 4-byte 0x00000001 first then 3-byte 0x000001. Verification: AVCC/Annex B/3-byte start code/empty/truncated/forbidden tests green. CGO_ENABLED=0 build+vet+tests green |
| VP9 detector: profile 3 reserved bit                   | Done    | 2026-07-12   | glm-5.2    | internal/core/codec_vp9.go: detector read frame_type at fixed bit 5, incorrect for profile 3 which inserts a reserved_zero bit shifting show_existing_frame/frame_type by one; rewrote with LSB-first bit reader (VP9 f(n) reads low bit first) handling profile==3 reserved bit. Verification: profile 0 KEY(0x02)/INTER(0x22) + profile 3 KEY(0x0E)/INTER(0x4E)/show_existing_frame(0x2E) spec-derived, MSB/LSB direction confirmed against VP9 spec section 9.2. CGO_ENABLED=0 build+vet+tests green |
| MP4 stream: chunk-exhaustion guard                     | Done    | 2026-07-12   | glm-5.2    | internal/format/mp4/stream.go: peekNext reused the last chunk's tail offset when samples exceeded the stco chunk table (inconsistent sample tables); added guard returning false (EOF) when chunkSampleLeft==0 and chunkIdx+1>=len(stco). Verification: 3-samples/1-chunk test yields 1 sample then EOF, no bogus offset read. CGO_ENABLED=0 build+vet+tests green |
| **Phase 6: Capability/Gate Layer**                   |         |              |            |                                                    |
| Container/codec capability + magic sniff gates        | Done    | 2026-06-30   | glm-5.2    | pkg/puremux capability.go: Container (WebM/MKV/MP4), SupportedInputs/Outputs, DetectContainer (EBML magic 0x1A45DFA3 + doctype sniff, MP4 ftyp/styp box), CanRemux (coarse container gate) + CanRemuxCodecs (per-codec gate), Merge(ctx,...)/MergeToWriter path-based entry. core: CodecVorbis/CodecFLAC/CodecAAC added; noopDetector registered for Vorbis/FLAC. Verification: spec-derived bytes (EBML magic 1A 45 DF A3, MP4 ftyp at offset 4, doctype sniff), boundary cases (empty/truncated/unknown magic -> ErrUnsupportedInput no panic), CanRemux/CanRemuxCodecs truth table, output-ext gate (.mp4/.avi rejected), AddTrack codec gate (FLAC rejected in WebM, allowed in MKV). MSB-first VINT confirmed. CGO_ENABLED=0 build+vet+tests green |
| MKV output + Vorbis/FLAC codec mapping                | Done    | 2026-06-30   | glm-5.2    | webm: WriteEBMLHeaderFor(doctype) selects webm/matroska; codecIDFor/codecTypeFromString extended with A_VORBIS/A_FLAC; Session.Config.OutputContainer drives doctype + AddTrack gate. Verification: Merge WebM->MKV round-trip asserts matroska doctype present + webm absent + V_VP9/A_OPUS present; AddTrack FLAC-in-WebM rejected / FLAC-in-MKV allowed. CGO_ENABLED=0 build+vet+tests green |
| **Phase 7: MP4 (ISOBMFF) Input Reader**             |         |              |            |                                                    |
| MP4 box walker (ftyp/moov/mdat demux)                 | Done    | 2026-06-30   | glm-5.2    | internal/format/mp4: ISOBMFF (ISO/IEC 14496-12) box walker, full moov/trak/mdia/minf/stbl parse (stsd/stts/stsz/stsc/stco/co64/stss), sample-table resolution (abs time from stts+mdhd timescale, keyframe from stss, offsets from stsc+stco, sizes from stsz), merged-time NextSample seeking into mdat. mp4_adapter wires it into the container-agnostic merge loop; DetectContainer sniffs ftyp/styp at offset 4. Verification: spec-derived bytes (box size BE, ftyp at off 4, co64 8-byte offsets, mdhd timescale off 8, stsd sample-entry type off 4), VP9/AV1/Opus/AAC/FLAC sample-entry mapping, 2-sample timecode (0/33ms) + keyframe flags confirmed, co64 round-trip, two-track merge order, boundary cases (truncated header, size overflow, no-moov, empty mdat, forbidden sample entry, garbage -> typed error no panic). MP4->WebM + MP4->MKV e2e via Merge; AAC-in-WebM rejected at AddTrack (per-track codec gate, not container gate). CGO_ENABLED=0 build+vet+tests green |
| **Phase 8: Codec Probe + Streaming + MPEG Codecs** |         |              |            |                                                    |
| Probe API (codec discovery)                          | Done    | 2026-06-30   | glm-5.2    | pkg/puremux probe.go: Probe(path) FormatInfo (container + per-track codec/kind), ProbeOutputContainer (picks WebM when all codecs fit, else MKV). Surfaces codecs directly so callers no longer need to guess container compatibility by extension alone. Verification: Probe on WebM(MK9+Opus)/MKV/MP4(VP9) asserts container + per-track codec+kind; Probe on H.264 MP4 detects CodecH264; ProbeOutputContainer picks WebM for VP9+Opus and MKV for H.264/FLAC; bad-input returns error. CGO_ENABLED=0 build+vet+tests green |
| H.264/HEVC codec + NAL keyframe detector            | Done    | 2026-06-30   | glm-5.2    | core: CodecH264/CodecHEVC + h264Detector/hevcDetector (NAL unit header parse, AVCC 4-byte length-prefix + Annex B start code 00 00 00 01/00 00 01 auto-detect). webm: V_MPEG4/ISO/AVC + V_MPEGH/ISO/SHEVC codec ID mapping (read+write). mp4: avc1/avc3/hvc1/hev1 sample-entry mapping. capability: H.264/HEVC added to MKV codec set (WebM-restricted). Verification: spec-derived NAL header bytes (H.264 IDR=0x65 forbidden=0 nal_ref_idc=3 type=5; HEVC IDR_N_LP type=20 byte0=0x28), AVCC + Annex B + 3-byte start code framing, multi-NAL packet (SPS+PPS+IDR), CRA/IRAP sync set 16-21, boundary cases (nil/empty/truncated/forbidden-bit/malformed-length -> no panic). MSB-first confirmed. CGO_ENABLED=0 build+vet+tests green |
| MP4 streaming parse (O(1) memory)                   | Done    | 2026-06-30   | glm-5.2    | internal/format/mp4 stream.go: replaced per-sample resolvedSample[] with streaming cursors (stts/chunk/stss cursors + samplePeek). Memory is O(tracks) not O(samples); peekNext computes the next sample on demand, advancePast moves cursors forward. Verification: multi-chunk (3 chunks, offset transitions), multi-stts-entry (varying deltas), audio no-stss (keyframe=false), uniform-size path; identical output to the old resolveTrack on all existing tests. CGO_ENABLED=0 build+vet+tests green |
| Build `cmd/puremux` (CLI wrapper)                     | Done    | 2026-06-30   | glm-5.2    | cmd/puremux: minimal entrypoint with -h/--help usage; thin shell over the library API. CGO_ENABLED=0 build green |
| **Phase 10: MPEG-TS Live Output + ES Ingestion**     |         |              |            |                                                    |
| core: ADTS header parse + AAC audio classification   | Done    | 2026-07-26   | claude-fable-5 | internal/core/codec_adts.go: ParseADTSHeader (syncword/layer/frame_length/freq-index/channel-config/raw-data-blocks per ISO 14496-3 §1.A.2.2), ForEachADTSFrame (concatenated-frame walk, 1-byte resync, truncated tail withheld), ADTSFrameInfo.Duration. CodecAAC added to IsAudio; read-only comment lifted (TS writes ADTS AAC). Verification: hand-derived header bytes (48kHz idx3 -> byte2 0x4C, frameLen 11 -> 01/7F; 44.1kHz idx4 -> 0x50), reserved index -> rate 0, boundary cases (nil/short/bad sync/layer!=0/len<7) no panic, resync + truncation + early-stop covered |
| preprocessor: MinMonotonicStep duplicate nudge       | Done    | 2026-07-26   | claude-fable-5 | Config.MinMonotonicStep (ns): Enforcer duplicate/backwards nudge is now max(1ns, step), shifting PTS by the same amount to preserve the PTS-DTS offset. Rationale: a 1ns nudge collapses back into a duplicate after quantization to coarse container clocks (90 kHz TS, 1ms Matroska), which is exactly the live-backfill "Invalid video timestamp" pathology. Verification: 5-packet duplicate burst at step=1ms emits DTS +1ms apart with PTS offset intact; zero-value keeps legacy 1ns behavior |
| internal/format/mpegts: TS live muxer                | Done    | 2026-07-26   | claude-fable-5 | Single-program ISO 13818-1 muxer implementing muxer.Muxer: dynamic PAT/PMT (stream_type 0x1B H.264 / 0x24 HEVC / 0x0F ADTS AAC), PCR on first video PID, PES with PTS-only (0b10) or PTS+DTS (0b11) headers, adaptation-field stuffing to exact 188B, per-PID continuity counters, periodic table re-emission, time.Duration -> 90kHz round-to-nearest rebased to first packet +10s headroom, 33-bit mask + negative clamp. Verification: CRC-32/MPEG-2 catalogue check value 0x0376E6E7, hand-derived PAT/PMT/PES-timestamp bytes (PTS 900000 -> 21 00 37 77 41), multi-packet PES round-trip, PCR only on video PID, continuity monotonic, tables re-emitted, empty payload + oversize PES (unbounded length 0) covered |
| Enforcer: stable insertion for equal DTS             | Done    | 2026-07-26   | claude-fable-5 | internal/preprocessor/enforcer.go: insertOrdered used sort.Search(DTS >= p.DTS) (first-equal index), inserting new packets BEFORE existing equal-DTS ones — a duplicate-stamped run emitted in REVERSED arrival order. Live impact (reproduced with a real x264 stream through the full pipeline + ffprobe): a backfill burst [IDR,F1..F9] all stamped t0 emitted as [F9..F1,IDR], the keyframe Aligner dropped all 9 frames ahead of the reordered IDR, and the TS rebase origin landed on the IDR's nudged (+9ms) DTS, shifting the whole timeline and producing residual startup DTS warnings downstream. Fixed to sort.Search(DTS > p.DTS) (insert after equals = stable FIFO). Verification: enforcer FIFO-among-equals test (payload-tagged), session e2e keyframe-first duplicate burst (no drops, IDR first at rebase origin 900000, strictly increasing PTS), ffmpeg -c copy mp4 remux of the repro stream now warning-free with all 150 AUs present (was 141). Also added TestLiveWriteCopiesCallerBuffer pinning the WriteVideo/WriteADTS payload-copy contract (callers may reuse their buffer immediately; zero-copy regressions fail the suite). CGO_ENABLED=0 build+vet+tests green |
| **Phase 11: B-frame DTS Synthesis (v0.0.7)**         |         |              |            |                                                    |
| preprocessor: DTSSynthesizer                          | Done    | 2026-07-27   | claude-fable-5 | internal/preprocessor/dtssynth.go: synthesizes DTS for decode-order video stamped with PTS only (live B-frame streams; DTS=PTS made decode timelines non-monotonic — field capture: 80/240 frames non-increasing pkt_dts_time, mpv A/V desync). Valid timeline = sorted PTS delayed by reorder depth; depth measured by bounded startup probe (hold ≤8 frames, mono early-exit at 4, +1 margin, cap 16 = H.264 DPB), then zero-latency steady state (min-heap of unconsumed PTS lagging `delay` frames); adaptive mid-stream depth growth with bounded documented DTS>PTS transient; extrapolated pre-roll clamped ≥0 (WebM uint ms math). delay==0 is exact passthrough so Enforcer keeps duplicate-PTS repair. Verification: unit tests pin exact IBBP timeline ([0,+1ms,10,20]ms), steady-state 1-emission-per-call (zero latency), mono passthrough DTS==PTS, dup burst untouched, short-stream Flush, mid-stream growth re-convergence; strict monotonic + DTS≤PTS + PTS-unmodified + decode-order invariants on every test. CGO_ENABLED=0 build+vet+tests green |
| facade: Session.WriteVideoReordered + Close drain     | Done    | 2026-07-27   | claude-fable-5 | pkg/puremux: WriteVideoReordered(trackID, au, pts) — PTS-only decode-order ingestion, per-track lazy DTSSynthesizer ahead of the Enforcer→Aligner pipeline; safe to call unconditionally (monotonic input byte-identical to WriteVideo). WritePacket split into closed-guard + writePacket so Close drains synthesizer probes (deterministic track order) before Enforcer/Aligner flush — a stream shorter than the probe still writes header+frames. Verification: session tests — B-frame IPBB×10 at 10s base: PES DTS strictly increasing at 90kHz, DTS≤PTS, decode-order ES, PTS deltas preserved, distinct PTS/DTS PES headers present; byte-identical monotonic + SOOP dup-burst vs WriteVideo; Close-drains-probe; audio interleave under probe hold. Out-of-repo ffmpeg e2e (scratchpad harness; repo bans os/exec): real x264 bframes=3:b-pyramid=normal 90 AUs fed PTS-only → ffprobe 0 non-monotonic DTS, 0 DTS>PTS, ffmpeg -c copy remux warning-free; no-B control identical-clean. CGO_ENABLED=0 build+vet+tests green |
| **Phase 13: POC-derived Presentation Timestamps (v0.0.9)** |         |              |            |                                                    |
| core: PictureOrderParser (H.264/HEVC POC)             | Done    | 2026-07-27   | claude-fable-5 | internal/core: rbspBitReader (MSB-first u/ue/se + 00 00 03 emulation-prevention removal), codec_h264_poc.go (SPS incl. high-profile chroma/scaling-list skip, PPS, slice header → POC type 0 per §8.2.1.1 with MSB wrap + ref-only prev + delta_pic_order_cnt_bottom min rule; type 2 = decode order; type 1 → per-AU unsupported), codec_hevc_poc.go (profile_tier_level skip, SPS/PPS, slice segment header → §8.3.1 with prevTid0 (tid==0, non-RASL/RADL/SLNR), IDR/BLA/first-CRA NoRaslOutputFlag resets, mid-stream CRA POC continuity), IDR epoch numbering, POCInfo.Before (epoch-major). forEachNAL refactor (scanNALs now wraps it). Verification: hand-derived spec fixtures (bit-exact SPS/PPS/slice bytes documented in poc_test.go; gradual-advance double-wraparound; non-ref B; epoch reset; EP bytes; nil/garbage/truncated no-panic), plus real-encoder ground truth: x264 240-frame b-pyramid AND x265 120-frame stream both sorted by (epoch,Order) match ffprobe display order exactly. CGO_ENABLED=0 build+vet+tests green |
| preprocessor: PresentationSynthesizer                 | Done    | 2026-07-27   | claude-fable-5 | internal/preprocessor/ptssynth.go: mirror of DTSSynthesizer — decode clock trusted as DTS, display timeline synthesized from POC: rank(f) = decodeIdx − a + b (a over 16-deep emitted ring, b over lookahead window), PTS = t[rank + D] (D = max displacement, preset from probe window so first emits use final delay — no colliding slots), slots beyond newest arrival extrapolated by median positive clock delta, per-frame PTS≥DTS clamp for jittery-clock degradation, non-picture AUs ride the FIFO with PTS==DTS, 8-picture probe (32-AU hard cap) → passthrough (zero steady latency, byte-identical) or reorder mode (lookahead bmax+2 ≤ 16, growIfLate widening). Verification: exact IPBB (slot d+1) and depth-2 pyramid (slot d+2) timelines, passthrough perCall, non-pic ordering, short-stream flush, jittery startup clock, mid-stream engagement re-convergence |
| facade: WriteVideo POC stage + Aligner param-set hold | Done    | 2026-07-27   | claude-fable-5 | pkg/puremux: WriteVideo routes MPEG-TS H.264/HEVC video through per-track ptsStage (POC parser + PresentationSynthesizer) ahead of Enforcer→Aligner; Close drains stages before synths (ordered tracks). Aligner: CodecConfigOnlyDetector (H.264/HEVC IsConfigOnly — param set present, no VCL) holds pre-keyframe SPS/PPS-only AUs (bounded 8) and replays them ahead of the first IDR; released on keyframe-less Flush/Reset. mpegts.AddTrack rejects >16 video / >32 audio (PES stream_id ranges, Table 2-18). Verification: session tests — exact repro (mono decode clock + 20-GOP B stream → PES DTS strictly increasing delta-exact to input, DTS≤PTS, decode order, display-rank PTS == slot rank+1 exact, split PTS/DTS present), 60-seed scrambled+dup startup burst deterministic, no-B stream stays PTS-only headers, standalone-SPS/PPS survival ahead of IDR; out-of-repo e2e (repo bans os/exec): real x264+AAC muxed A/V TS → ffprobe 0 non-monotonic DTS both streams, 0 DTS>PTS, decoded display PTS strictly increasing exact 60fps, ffmpeg -f null zero warnings; go test -race (amd64) green; copy-contract + pool ownership audited. CGO_ENABLED=0 build+vet+tests green |
| **Phase 14: Compressed Media Toolkit Expansion**     |         |              |            |                                                    |
| 1. Architecture + public contract design             | Done    | 2026-09-03   | Codex /root | Added `pkg/media` contracts for exact rational time bases, explicit known/unknown timestamps, stream/codec/config metadata, context-aware demux/seek, idempotent packet release, and sequential/random-access source capabilities; added file and memory sources while preserving `pkg/puremux`. Expanded architecture without relaxing the no-codec boundary and retained the module path for ytv1/lavalink compatibility. Verification: API boundary tests cover exact 48 kHz conversion, overflow/invalid rationals, known zero timestamps, double release, random access, cancellation, close behavior; `CGO_ENABLED=0 go test ./pkg/media`, `go vet ./pkg/media`, and pre-change full baseline green. |
| 2. WebM/Matroska Opus demux completion               | Done | 2026-09-03 | Codex /root | Added packet-accurate WebM/Matroska demuxing with nanosecond timestamps, BlockGroup duration/reference/discard-padding, no/Xiph/fixed/EBML lacing, Opus TOC duration, CodecDelay/SeekPreRoll/DefaultDuration, track metadata/tags, Cues plus Cluster fallback seek, and the `pkg/media` adapter. Verification: official libwebm `discard_padding.webm` fixture validates three BlockGroups and signed padding; RFC 6716-derived TOC bytes cover SILK/CELT, codes 0-3 and 120 ms maximum; Matroska-derived lacing bytes confirm VINT/MSB-first sizes and signed-bias deltas; seek-after-EOF, cancellation, nil/truncated/zero-track/negative-size/overrun cases are covered without panics. `CGO_ENABLED=0 go test ./...` and full `go vet ./...` green. |
| 3. HTTP range source + cancellation                  | Done | 2026-09-03   | Codex /root | Added seekable `HTTPSource` using caller-owned clients/headers, explicit bounded Range requests, exact `Content-Range`/size/content-encoding validation, ETag/Last-Modified `If-Range`, typed source-change and non-seekable errors, caller retry policy, context cancellation, and Close-driven request cancellation. Verification: isolated `httptest` servers cover random/sequential/short reads, seek, custom headers, conditional requests, 503 retry, changed entities, ignored/malformed ranges, negative offsets, close, and cancellation unblocking; `CGO_ENABLED=0 go test ./...` and full `go vet ./...` green. (`-race` is unavailable under the required CGO-disabled build.) |
| 4. Lavalink non-CGO Opus passthrough                 | Done | 2026-09-03   | Codex /root | Replaced Lavalink's `!cgo` all-disabled stub with a `pkg/media`-backed WebM/Matroska reader for files and HTTP ranges; Opus PTS is normalized to 48 kHz ticks for the existing pacing path, seek/position/metadata/codec discovery are preserved, packets retain explicit release ownership, and filter/transcode failures use distinct exported errors. The loopback stream proxy now honors bounded `bytes=N-M` requests so the random-access source works for ytv1-spooled media. Demux reads propagate operation contexts and Close can interrupt an in-flight source read. Verification: official libwebm Opus fixture opens, selects Opus, passes the 10-byte packet unchanged, releases idempotently, and seeks/re-reads; range parser/206/416 proxy tests and capability-error tests pass. `CGO_ENABLED=0` audio/player/httpproxy tests + vet and puremux full tests + vet green. Lavalink's unrelated full-suite `TestPathTraversal` remains environment-skipped-by-failure because Windows symlink privilege is unavailable. |
| 5. Ogg/Opus demux                                    | Done | 2026-09-03   | Codex /root | Added Ogg page parsing with sequence/serial/continuation validation, segment-table packet reconstruction across pages, CRC-32/Ogg verification, OpusHead channel/pre-skip/mapping parsing, bounded OpusTags comments, 48 kHz granule/PTS/duration calculation, page index seek, and `pkg/media` auto-probe/adapter. Verification: the first page of Xiph's official `speech_music_test.opus` validates real capture/header/CRC bytes and OpusHead values; spec-derived Ogg pages validate little-endian fields, granule 312+2*960, pre-skip removal, RFC 6716 TOC 0xF8 duration, metadata and seek replay. CRC polynomial 0x04C11DB7 is processed MSB-first; nil/truncated/bad CRC/bad continuation/oversized tags/malformed head cases are covered without panics. `CGO_ENABLED=0 go test ./...`, full vet, and diff check green. |
| 6. Progressive MP4/M4A demux completion              | Done | 2026-09-03   | Codex /root | Extended the O(1)-cursor MP4 reader with exact DTS/PTS/duration/timebase packets, ctts v0/v1 (including signed offsets), mvhd/mdhd/tkhd duration/ID/language, edit-list state, audio/video sample-entry fields, avcC/hvcC/av1C/vpcC/dOps/dfLa configs, bounded esds ASC extraction, common iTunes metadata, multi-track exact ordering, sync-sample seek, and the public MP4 adapter/probe. Verification: ISO BMFF hand-derived boxes validate ctts v1 offsets 80/-40/-40, DTS 0/40/80 versus PTS 80/0/40, 1920x1080 avcC, ISO-639 packed `eng`, movie/track duration, metadata and keyframe seek; AAC-LC ASC `0x12 0x10` confirms MSB-first AudioSpecificConfig fields, edit-list empty/media entries and malformed descriptor/ctts bounds are covered. Public dOps/Opus M4A test preserves packet/ticks/config. Full `CGO_ENABLED=0 go test ./...` and vet green before the final focused boundary rerun; focused MP4 tests/vet green after it. |
| 7. Fragmented MP4 demux                              | Done | 2026-09-03   | Codex /root | Added `mvex/trex` defaults and `moof/traf/tfhd/tfdt/trun/mdat` sample resolution with signed data/CTO fields, explicit/default duration-size-flags, default-base-is-moof, exact decode/presentation ticks, sync flags, inter-track fragment ordering and fragment seek. `NewFragmentReader` plus public `OpenMP4WithInit` retain a separate initialization segment's track/config state; combined init+media files also auto-open. Verification: hand-derived ISO BMFF init and two-sample Opus fragment exercise trex defaults, 64-bit tfdt, trun data offset relative to moof, exact 0/960 ticks and byte offsets, seek/re-read, combined-file open, and truncated mdat rejection before out-of-bounds reads. Focused non-CGO MP4/media tests and vet green. |
| 8. Optional FFmpeg packet decoder bridge             | Done | 2026-09-03 | Codex /root | Lavalink now exposes `DecoderConfigFromMedia`, `OpenDecoderConfig`, `NewCompressedPacket`, and `NewTranscoderForConfig`; the CGO backend copies padded codec extradata and Go packet bytes into FFmpeg-owned buffers and preserves PTS/DTS/duration without requiring AVFormatContext. The `!cgo` backend returns typed filter/transcode capability errors. Native verification uses FFmpeg 8.1/libavcodec 62 headers and DLLs with a real encoder-produced AAC-LC 48 kHz stereo ADTS fixture: three stripped compressed access units traverse packet allocation, decoder, filter/resample, FIFO, and Opus encoder, then flush yields non-empty packets. This exposed and fixed an existing Windows heap crash: `AVFilterInOut.name` had been allocated by `C.CString` but released by `av_free`; names now use `av_strdup` behind C helpers so allocator ownership matches. Focused CGO `go test` and vet are green; non-CGO ownership/config/error and audio/player suites remain green. |
| 9. H264/HEVC/AAC bitstream framing filters           | Done | 2026-09-03   | Codex /root | Added public allocation-bounded NAL framing plus H.264 AVCDecoderConfigurationRecord, HEVC HEVCDecoderConfigurationRecord, and AAC AudioSpecificConfig/ADTS parsers and two-way converters. Parameter-set prepend is explicit; payloads remain compressed and opaque. Verification: spec-derived big-endian 1/2/4-byte NAL lengths and 3/4-byte Annex-B codes round-trip; AVC SPS/PPS/IDR headers and HEVC VPS/SPS/PPS/IDR_W_RADL arrays are hand-derived; AAC-LC/44.1kHz/stereo ASC `12 10` is re-derived MSB-first and produces the exact 10-byte-frame ADTS header `FF F1 50 80 01 5F FC`. Nil/empty/truncated/zero-length/overrun/reserved-length/reserved-frequency/oversized-frame cases are covered without panics. Full `CGO_ENABLED=0 go test ./...` and vet green. |
| 10. MP3/ADTS/FLAC raw demux                          | Done | 2026-09-03   | Codex /root | Added indexed raw ADTS, MPEG audio and native FLAC demuxers with exact sample-clock PTS/duration, packet seek, ID3v2 text metadata, Vorbis comments, AAC ASC and FLAC STREAMINFO codec configuration. ADTS headers are stripped while compressed access units remain opaque; MP3 and FLAC frames are preserved byte-for-byte. Verification: ISO MPEG Audio MSB-first header `FF FB 90 64` re-derives MPEG-1 Layer III 128 kb/s/44.1 kHz/417 bytes/1152 samples and MPEG-2 `FF F3 86 C0` re-derives 64 kb/s/24 kHz/193 bytes/576 samples; RFC 9639 STREAMINFO bit packing and fixed-block FLAC frame header/CRC-8 are hand-derived; AAC `12 10`, ID3, Vorbis comments, packet offsets, duration and seek are exercised end-to-end. Nil/truncated, bad sync/CRC, reserved version/layer/bitrate/rate/emphasis/block/sample fields and frame/metadata overruns are covered without panics. Focused tests and vet green. |
| 11. HLS manifest demux                               | Done | 2026-09-03   | Codex /root | Added bounded RFC 8216 master/media parsing and `OpenHLS`: highest-bandwidth/caller variant selection, DEFAULT audio renditions, URI resolution, media sequence, fractional EXTINF, target duration, EXT-X-MAP, explicit/implicit BYTERANGE, discontinuity flags, ENDLIST seek, lazy segment fetching, live playlist refresh, caller headers/context/limits, strict 206 Content-Range validation, and AES-128 CBC with explicit or sequence-derived IV plus PKCS#7 validation/key caching. Segment bytes delegate to ADTS/MP3/FLAC/Ogg/WebM, fMP4-with-init, or the new single-program MPEG-TS demuxer. The TS reader validates 188-byte transport headers, continuity, PAT/PMT CRC, PES markers/timestamps and extracts opaque AAC/MP3/H.264/HEVC packets. Lavalink's non-CGO `OpenMedia` routes `.m3u8`/explicit HLS and `.mpd`/explicit DASH URLs to the manifest demuxers while preserving request headers and packet DTS/duration. Verification: RFC-field playlists cover quoted master attributes, audio group choice, map/ranges including implicit offset, sequence 42, discontinuity and 128-bit IV; AES tests decrypt two independently padded segments with big-endian media-sequence IVs and verify the second segment's global 44.1 kHz timestamp/discontinuity/seek; live refresh advances sequence without replay. ISO 13818-1 PTS bytes `21 00 37 77 41` re-derive 900000 and writer-produced PAT/PMT/PES round-trip an ADTS AAC access unit. Nil/truncated, malformed attributes/ranges/IVs, unsupported encryption, size limits, bad marker/sync/alignment and cancellation are covered. Full puremux `CGO_ENABLED=0 go test ./...`/vet and focused Lavalink non-CGO audio/player/proxy tests/vet green. |
| 12. DASH manifest demux                              | Done | 2026-09-03   | Codex /root | Added bounded MPD parsing and `OpenDASH` with hierarchical BaseURL resolution, AdaptationSet-to-Representation inheritance (including SegmentBase/List/Template), default highest-bandwidth audio/caller selection, ISO-8601 media/period duration, SegmentBase initialization/index ranges, SegmentList initialization/media ranges, SegmentTemplate `$RepresentationID$`/`$Bandwidth$`/formatted `$Number$`/`$Time$`/`$$`, SegmentTimeline `t/d/r` including bounded `r=-1`, presentation-time-offset, lazy HTTP segment fetch, fMP4 init delegation, single-file range-source fallback, global packet timing and static seek. All tick/duration/count/number arithmetic is overflow-checked; unsupported multi-Period MPDs fail explicitly instead of selecting a later period incorrectly. Verification: spec-shaped MPDs exercise four inherited 48 kHz timeline segments (formatted number 00007, PTO-relative 0..3 s), list/base inclusive ranges, nested BaseURL, fractional ISO duration, representation selection, strict ranged HTTP responses and two ADTS segments with exact 0/48000 timestamps plus seek/re-read. Wrong root/empty XML or Period, reversed ranges, unknown tokens, unbounded negative repeat, zero duration, arithmetic/manifest/representation/segment limits and canceled opens are rejected. Final uncached full `CGO_ENABLED=0 go test ./...` and full vet green. |
| v0.1.0 release gate                                 | Done | 2026-09-03   | Codex /root | All twelve compressed-media toolkit expansion items are complete. Release verification: uncached `CGO_ENABLED=0 go test ./...`, full `go vet ./...`, `git diff --check`, changed-file gofmt audit, and `go mod tidy -diff` are green. Lavalink integration is independently verified under non-CGO, and its optional FFmpeg 8.1 CGO packet bridge passes AAC-to-Opus execution tests. |
| Post-v0.1 performance and API contract hardening    | Done | 2026-09-04 | Codex /root | Rational Duration/Rescale now use allocation-free fixed-width 192/128-bit integer arithmetic with GCD cancellation; deterministic randomized cases match a math/big oracle across signed limits and AllocsPerRun confirms zero heap allocations. MP4 timestamp ordering/scaling also avoids math/big and its signed cross-product comparator matches an oracle with zero allocations. Open accepts non-seekable MPEG-TS with an explicit hint, makes no speculative read when the required hint is absent, and retains random-access requirements for indexed formats; writer-produced PAT/PMT/PES plus spec-derived ADTS verifies sequential ingestion and opaque AAC preservation. MaxProbeBytes is validated and strictly caps detection reads. SeekFlags now have defined forward/backward/any semantics across raw, MP4/fMP4, MPEG-TS, Ogg, WebM and manifest delegation; unknown bits fail explicitly, direction/SeekAny tests cover exact packet choices, and RFC 6716-derived two-page Ogg packets verify page-start forward/backward selection. Verification: uncached `CGO_ENABLED=0 go test ./...`, `go vet ./...`, `go mod tidy -diff`, and `git diff --check` are green. |
| Audit remediation 1: destructive I/O and ownership safety | Done | 2026-09-04 | Codex /root | Fixed exact-four-byte EBML detection panic; path Merge now rejects input/output aliases and writes through a same-directory temporary file while preserving an existing destination on failure; Session copies caller-owned packets before buffering/pooling, validates nil writers/track kinds/numeric metadata, copies codec private data, and rejects unsupported non-1ms EBML scales while matching the monotonic nudge to the serialized clock. Short writes now surface `io.ErrShortWrite`. Verification: focused facade tests cover exact truncated bytes with no panic, same-file source preservation, failed-remux destination preservation, caller mutation after WritePacket, nil/scale validation, and legal short-writer behavior; `CGO_ENABLED=0 go test -count=1 ./pkg/puremux` green. |
| Audit remediation 2: retry-safe indexed reads | Done | 2026-09-04 | Codex /root | Raw ADTS/MP3 now reject zero-frame hinted inputs instead of creating an invalid zero-timescale demuxer; raw Seek checks closed/empty state; raw and progressive/fragmented MP4 cursors commit only after successful sample reads, so cancellation/transient I/O does not skip data. Removed the per-ADTS-frame synthetic full-frame allocation while preserving header/config extraction. Verification: empty hinted inputs and seek-after-close return typed errors without panic; injected one-shot read failures in raw ADTS and progressive MP4 re-read the exact first spec-shaped frame/sample on retry; `CGO_ENABLED=0 go test -count=1 ./pkg/media ./internal/format/mp4` green. |
| Audit remediation 3: manifest bounds and encryption state | Done | 2026-09-04 | Codex /root | HLS rejects missing/zero/non-finite/overflowing EXTINF and target durations, bounds renditions, checks media-sequence/global-time arithmetic, captures the encryption key effective at each EXT-X-MAP declaration, and refetches AES keys so same-URI rotation cannot reuse stale bytes. Init caching now includes key/IV identity and stream-transition checks include dimensions plus codec configuration. DASH rejects zero-duration SegmentLists, caps template formatting width at 64 digits, and checks seek/packet timestamp arithmetic; HLS/DASH reject a MaxInt64 resource limit before `limit+1` overflow. Verification: RFC-shaped playlists cover missing/zero/NaN/Inf EXTINF, rendition bounds, and key-before-map/key-reset semantics; MPDs cover zero list duration and hostile billion-digit format widths; `CGO_ENABLED=0 go test -count=1 ./internal/manifest ./pkg/media` green. |
| Audit remediation 4: codec sync detection and concurrent release | Done | 2026-09-04 | Codex /root | VP8 sync detection now requires the complete 10-byte uncompressed key-frame header and `9D 01 2A` start code; VP9 profile 3 rejects nonzero `reserved_zero`; AV1 rejects OBU/extension reserved bits, bounds LEB128 before int conversion, and tracks the sequence-header `reduced_still_picture_header` implied-key-frame mode. DetectorRegistry uses an RWMutex, public media Packet release uses atomic once semantics, and the internal pool discards backing arrays above 1 MiB. Verification: RFC 6386-derived VP8 frame tag/start code, VP9 MSB-first profile-3 bit, and AV1 MSB-first sequence/OBU fields are hand-derived in tests; nil/truncated/bad-sync/reserved cases do not panic; 32 concurrent Release calls invoke the callback exactly once; `CGO_ENABLED=0 go test -count=1 ./internal/core ./pkg/media` green. |
| **Phase 12: Jitter-robust DTS Synthesis (v0.0.8)**   |         |              |            |                                                    |
| preprocessor: continuous window DTS measurement       | Done    | 2026-07-27   | claude-fable-5 | internal/preprocessor/dtssynth.go: the v0.0.7 one-shot startup probe was non-deterministic on live backfill bursts (out-of-order/duplicated first frames): 4 "non-decreasing" duplicates mis-locked DTS==PTS passthrough (field capture: ~80/240 dup/non-monotonic DTS for the rest of the stream), or a scrambled burst locked a permanently inflated delay. Now continuous: probe always evaluates the full 8-frame window (order-tolerant measurement, no mono early-exit); passthrough requires ≥4 distinct PTS (dup-heavy window extends probe, cap 32); reordered dup runs size delay ≥ run length so dup DTS stays ≤ PTS; steady phase decays delay one step per 16-frame evidence window (skip-ahead pops in sorted-PTS timeline: monotonic + DTS≤PTS preserved) alongside the existing immediate growth. Verification: 100-seed scrambled-burst determinism (unit + full-session TS, per-frame strict-mono/DTS≤PTS/decode-order/PTS-delta assertions), dup-burst-then-B extension, dup-only cap passthrough, reversed-window decay convergence, all v0.0.7 tests (byte-identical mono + SOOP dup paths, exact IBBP timeline, zero steady latency) still green; ffprobe 0 non-monotonic pkt_dts_time + ffmpeg remux warning-free including startup. CGO_ENABLED=0 build+vet+tests green |
| capability + facade: ContainerMPEGTS, live ES API    | Done    | 2026-07-26   | claude-fable-5 | ContainerMPEGTS (write-only; H264/HEVC/AAC) in capability tables; Merge/MergeToWriter explicitly refuse TS output (file inputs are AVCC/raw-AAC framed; no framing conversion under §4). pkg/puremux/types.go: public Packet/CodecType aliases + codec consts so external repos can feed packets. Session: OutputContainer=MPEGTS backend branch (writeHeader/writeBlock/Close), resolveAudioSync extracted. live.go: WriteVideo (copies AU, keyframe via detector registry so audio sync latches) + WriteADTS (per-frame split, PTS advanced by ADTS frame duration). Verification: e2e live-repair test — pre-keyframe drop, duplicate burst 160x3 -> +1ms nudge, out-of-order 200/180 reordered; parse-back asserts strictly increasing 90kHz PES PTS, IDR-first ES, ADTS spacing exactly 1920 ticks; codec gate rejects VP9/Opus in TS. CGO_ENABLED=0 build+vet+tests green (all packages) |
